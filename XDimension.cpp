#include <windows.h>
#include <string>
#include <vector>

int main(int argc, char const *argv[])
{
	// Start the code
	int start;
	for (int start = 0; start < 1; ++start)
	{
		int xdim1;
		printf("Loading the XDimension...\n");
	}
	int xdim2 = 1;
	int xmax = 360;
	int xmin = 1;
	if (xmax == 361)
	{	
		MessageBox(NULL, "Cannot render your Command Prompt more beatiful ;(", "ERROR", MB_ICONERROR);
	}else{
		printf("XXXXXXXXXXXXXXXXXXXXX\n");
		printf("XXXXXXX_______XXXXXXX\n");
		printf("XXXXXXX       XXXXXXX\n");
		printf("XXXXXXX     . XXXXXXX\n");
		printf("XXXXXXX       XXXXXXX\n");
		printf("XXXXXXX       XXXXXXX\n");
		printf("XXXXXXX_______XXXXXXX\n");
		printf("XXXXXXXXXXXXXXXXXXXXX\n");
		printf("The closed door.\n");
		MessageBox(NULL, "CONGRATULATION: You can now support X!", "INFO", MB_ICONWARNING);
		int y1;
	}
	int y2;
	int ymin = 0;
	int ymax = 180;
	if (ymax == 181) {
		MessageBox(NULL, "An error occured!", "ERROR", MB_ICONERROR);
	}else{
		printf("All works. You can now seen the beatiful graphics like HALO Infinite in your Command Prompt!\n");
	}
	// Return 0
	return 0;
}